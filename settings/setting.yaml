GLOBAL_TIMEOUT                : 20s
api_key                       : null
default_headers:
  Content-Type: application/json
  Accept: application/json

Token                         : "Test"

API: 
  url: 
    dev: https://valentinos-coffee.herokuapp.com
  status: /status
  product: /products
  single_product: /products/{product_id}
  register: /clients
  order: /orders
  single_order: /orders/{order_id}